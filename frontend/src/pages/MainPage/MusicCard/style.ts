import { css } from '@emotion/react';

export const s_img = css`
  border-radius: 24px;
  width: 100%;
  height: 100%;
  cursor: pointer;
  transition: 0.2s;
  :hover {
    filter: brightness(0.5);
  }
`;
